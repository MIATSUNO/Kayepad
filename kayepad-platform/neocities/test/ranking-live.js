(()=>{const API=window.KAYSTANT_API||'',q=s=>document.querySelector(s),esc=s=>String(s||'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));const labels={active:'Mais assíduos',authors:'Autores mais prolíficos',ink:'Maiores apoiadores'};const load=async cat=>{const box=q('.ranking');if(!box)return;box.innerHTML='<li class="real-empty">Carregando ranking...</li>';const r=await fetch(API+'/rankings?category='+cat);const data=await r.json().catch(()=>[]);box.innerHTML=data.length?data.map((x,i)=>`<li><strong>${i+1}</strong><span class="rank-avatar">${esc((x.user.username||'K')[0].toUpperCase())}</span><div><b>@${esc(x.user.username)}</b><small>${esc(x.label)}</small></div></li>`).join(''):'<li class="real-empty">Ainda não há dados para este ranking.</li>'};const panel=document.querySelector('.ranking')?.closest('.panel');if(panel){const title=panel.querySelector('.panel-title');const controls=document.createElement('div');controls.className='ranking-tabs';Object.entries(labels).forEach(([k,v],i)=>{const b=document.createElement('button');b.type='button';b.dataset.rank=k;b.textContent=v;b.className=i?'':'active';b.onclick=()=>{controls.querySelectorAll('button').forEach(x=>x.classList.remove('active'));b.classList.add('active');load(k)};controls.append(b)});title.after(controls);load('active')}})();