(()=>{if(window.__kayStudioReady)return;window.__kayStudioReady=true;const API=window.KAYSTANT_API||'',token=()=>localStorage.getItem('kaystant_token')||'',toast=m=>{const t=document.querySelector('#toast');if(t){t.textContent=m;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2400)}};const tabs=[...document.querySelectorAll('.tab')],panels=[...document.querySelectorAll('.creation-panel')];const lock=(panel,tab,label)=>{if(tab)tab.hidden=true;if(panel){panel.classList.add('hidden');const msg=document.createElement('div');msg.className='locked-feature';msg.innerHTML=`<p class="eyebrow">recurso bloqueado</p><h2>${label}</h2><p>Compre este item na Ke-Shop para desbloquear esta função.</p><a class="btn primary" href="index.html#loja">Ir para a Ke-Shop</a>`;panel.replaceWith(msg)}};async function init(){if(!token()){tabs.forEach(x=>x.hidden=true);panels.forEach(x=>x.classList.add('hidden'));const n=document.createElement('section');n.className='locked-feature';n.innerHTML='<p class="eyebrow">área de criação</p><h2>Entre para criar</h2><p>Entre na sua conta e compre a Caneta ou a Régua na Ke-Shop para desbloquear estas funções.</p><a class="btn primary" href="account.html">Entrar</a>';document.querySelector('.studio-tabs')?.after(n);return}const r=await fetch(API+'/me/unlocks',{headers:{Authorization:'Bearer '+token()}});if(!r.ok)return;const d=await r.json(),caneta=d.items.caneta,regua=d.items.regua;const b=panels.find(x=>x.id==='book'),g=panels.find(x=>x.id==='group');if(!caneta)lock(b,tabs.find(x=>x.dataset.panel==='book'),'Publicação de livros');if(!regua)lock(g,tabs.find(x=>x.dataset.panel==='group'),'Criação de mini-clubes');document.querySelectorAll('.tab:not([hidden])').forEach(x=>x.onclick=()=>{tabs.forEach(y=>y.classList.remove('active'));panels.forEach(y=>y.classList.add('hidden'));x.classList.add('active');document.querySelector('#'+x.dataset.panel)?.classList.remove('hidden')});const list=document.createElement('section');list.className='studio-library';list.innerHTML='<div><p class="eyebrow">conteúdo desbloqueado</p><h2>Livros e mini-clubes</h2></div><div class="library-grid" id="library-grid">Carregando...</div>';document.querySelector('.studio')?.append(list);const [books,groups]=await Promise.all([fetch(API+'/books').then(x=>x.json()).catch(()=>[]),fetch(API+'/groups').then(x=>x.json()).catch(()=>[])]);const box=list.querySelector('#library-grid');const cards=[...books.map(x=>`<article class="library-card">${x.cover_url?`<img src="${x.cover_url}" alt="Capa de ${x.title}">`:''}<h3>${x.title}</h3><p>${x.description||''}</p></article>`),...groups.map(x=>`<article class="library-card">${x.image_url?`<img src="${x.image_url}" alt="Imagem do grupo ${x.name}">`:''}<h3>${x.name}</h3><p>${x.hashtag} · ${x.members}/10 membros</p></article>`)];box.innerHTML=cards.length?cards.join(''):'<p class="muted">Nenhum livro ou mini-clube publicado ainda.</p>'}init()})();